# ════════════════════════════════════════════════════════════════════════════
# SCAFFOLD — EAS native build / submit / OTA update for the Expo app (apps/app).
# ════════════════════════════════════════════════════════════════════════════
#
# THIS IS A SCAFFOLD, NOT A FINISHED PIPELINE. It needs Expo credentials the
# OWNER provides and an `eas.json` profile decision the owner finalizes:
#
#   • REQUIRED SECRET (Settings → Secrets and variables → Actions → Secrets):
#       EXPO_TOKEN   — an Expo access token (expo.dev → Account → Access Tokens).
#                      Without it, `eas build/update/submit` cannot authenticate.
#
#   • REQUIRED before `submit` works: store credentials configured on EAS
#       (Apple App Store Connect API key / Google Play service-account JSON),
#       set up via `eas credentials` or the eas.json `submit` profile. The
#       submit job below is gated OFF by default (run it only once creds exist).
#
#   • eas.json profiles (apps/app/eas.json) currently define `development`,
#       `preview`, and `production` build profiles + an empty `submit.production`.
#       The owner should confirm the staging channel name used for OTA updates
#       (this scaffold uses `staging`) and finalize the submit profile.
#
# Monorepo note: EAS auto-detects the workspace; the working-directory is
# apps/app and `eas build` resolves the root lockfile. No app-source changes here.
#
# TRIGGER: workflow_dispatch ONLY (with inputs to pick platform / action).

name: Deploy App Native (EAS) [scaffold]

on:
  workflow_dispatch:
    inputs:
      action:
        description: "What to run"
        required: true
        default: "update"
        type: choice
        options:
          - build
          - update
          - submit
      platform:
        description: "Target platform (for build/submit)"
        required: true
        default: "all"
        type: choice
        options:
          - all
          - android
          - ios
      profile:
        description: "EAS build profile (from apps/app/eas.json)"
        required: true
        default: "preview"
        type: choice
        options:
          - development
          - preview
          - production
      channel:
        description: "EAS Update channel (for the update action)"
        required: true
        default: "staging"
        type: string

permissions:
  contents: read
  id-token: write

jobs:
  eas:
    name: EAS ${{ inputs.action }}
    runs-on: ubuntu-latest
    env:
      EXPO_PUBLIC_EAS_PROJECT_ID: 0039c25f-f88e-491d-8da9-85b8d6e66558
      NODE_PATH: ${{ github.workspace }}/node_modules:${{ github.workspace }}/apps/app/node_modules
    defaults:
      run:
        working-directory: apps/app
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6
        with:
          node-version: "22"
          cache: npm

      - name: Set up EAS / Expo
        uses: expo/expo-github-action@eab7a230208c952974db8c3245cfd78402c7b385 # v9
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install (root, workspace-aware)
        working-directory: .
        run: npm ci

      - name: Verify Expo config
        if: ${{ inputs.action == 'build' }}
        run: |
          node -e "console.log(require.resolve('expo/config-plugins'))"
          npx expo config --json > "$RUNNER_TEMP/expo-config.json"

      # ── build: produces native binaries on EAS (managed credentials) ────────
      - name: EAS Build
        id: eas-build
        if: ${{ inputs.action == 'build' }}
        run: |
          eas build \
            --non-interactive \
            --json \
            --platform ${{ inputs.platform }} \
            --profile ${{ inputs.profile }} \
            > "$RUNNER_TEMP/eas-build.json" || {
              cat "$RUNNER_TEMP/eas-build.json"
              exit 1
            }

      - name: Capture Android build artifact URL
        if: ${{ inputs.action == 'build' && inputs.platform != 'ios' }}
        env:
          EAS_BUILD_JSON: ${{ runner.temp }}/eas-build.json
        run: |
          node <<'NODE'
          const fs = require("fs");

          const raw = fs.readFileSync(process.env.EAS_BUILD_JSON, "utf8").trim();
          const parsed = JSON.parse(raw);
          const builds = Array.isArray(parsed)
            ? parsed
            : Array.isArray(parsed.builds)
              ? parsed.builds
              : [parsed];
          const androidBuild =
            builds.find((build) => String(build.platform || "").toLowerCase() === "android") || builds[0];
          const artifactUrl =
            androidBuild?.artifacts?.applicationArchiveUrl ||
            androidBuild?.artifacts?.buildUrl ||
            androidBuild?.artifacts?.apkUrl ||
            androidBuild?.buildUrl ||
            "";

          if (!artifactUrl) {
            console.error("EAS build completed but no Android artifact URL was present.");
            console.error(JSON.stringify(androidBuild, null, 2));
            process.exit(1);
          }

          fs.appendFileSync(process.env.GITHUB_ENV, `EAS_BUILD_ID=${androidBuild.id || ""}\n`);
          fs.appendFileSync(process.env.GITHUB_ENV, `EAS_BUILD_ARTIFACT_URL=${artifactUrl}\n`);
          NODE

      - name: Download Android build artifact
        if: ${{ inputs.action == 'build' && inputs.platform != 'ios' }}
        run: |
          mkdir -p "$RUNNER_TEMP/eas-artifacts"
          curl --fail --location "$EAS_BUILD_ARTIFACT_URL" \
            --output "$RUNNER_TEMP/eas-artifacts/gogocash-${{ inputs.profile }}-android.apk"

      - name: Upload Android build artifact
        if: ${{ inputs.action == 'build' && inputs.platform != 'ios' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: gogocash-${{ inputs.profile }}-android
          path: ${{ runner.temp }}/eas-artifacts/gogocash-${{ inputs.profile }}-android.apk
          if-no-files-found: error

      - name: Authenticate to Google Cloud for Android artifact archive
        if: ${{ inputs.action == 'build' && inputs.platform != 'ios' && vars.GCP_EAS_ARTIFACT_BUCKET != '' }}
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          workload_identity_provider: ${{ vars.GCP_WIF_PROVIDER }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT }}

      - name: Set up Google Cloud SDK for Android artifact archive
        if: ${{ inputs.action == 'build' && inputs.platform != 'ios' && vars.GCP_EAS_ARTIFACT_BUCKET != '' }}
        uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3

      - name: Archive Android build artifact to GCS
        if: ${{ inputs.action == 'build' && inputs.platform != 'ios' && vars.GCP_EAS_ARTIFACT_BUCKET != '' }}
        env:
          APK_PATH: ${{ runner.temp }}/eas-artifacts/gogocash-${{ inputs.profile }}-android.apk
          GCS_BUCKET: ${{ vars.GCP_EAS_ARTIFACT_BUCKET }}
          GCS_PREFIX: eas/android/${{ inputs.profile }}/${{ github.run_id }}
        run: |
          SHA_PATH="${APK_PATH}.sha256"
          sha256sum "$APK_PATH" | tee "$SHA_PATH"

          gcloud storage cp "$APK_PATH" "gs://${GCS_BUCKET}/${GCS_PREFIX}/gogocash-${{ inputs.profile }}-android.apk"
          gcloud storage cp "$SHA_PATH" "gs://${GCS_BUCKET}/${GCS_PREFIX}/gogocash-${{ inputs.profile }}-android.apk.sha256"

          {
            echo "### Android APK archive"
            echo "- APK: \`gs://${GCS_BUCKET}/${GCS_PREFIX}/gogocash-${{ inputs.profile }}-android.apk\`"
            echo "- SHA-256: \`$(cut -d ' ' -f1 "$SHA_PATH")\`"
          } >> "$GITHUB_STEP_SUMMARY"

      # ── update: OTA JS/asset update to a channel (no store review) ──────────
      #   Pass user-provided inputs via env (not inline) to avoid shell-injection
      #   from the free-text channel input.
      - name: EAS Update (OTA)
        if: ${{ inputs.action == 'update' }}
        env:
          EAS_CHANNEL: ${{ inputs.channel }}
          COMMIT_SHA: ${{ github.sha }}
        run: |
          eas update --non-interactive --branch "$EAS_CHANNEL" --message "ci: $COMMIT_SHA"

      # ── submit: upload a finished build to the stores ───────────────────────
      #   GATED: requires store credentials configured on EAS first (see header).
      #   Leave this until the owner finalizes the eas.json submit profile.
      - name: EAS Submit
        if: ${{ inputs.action == 'submit' }}
        run: eas submit --non-interactive --platform ${{ inputs.platform }} --profile ${{ inputs.profile }}
