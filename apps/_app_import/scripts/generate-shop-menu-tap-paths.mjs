#!/usr/bin/env node
/**
 * Placeholder for automating SVG path export from Figma (8123:68217).
 * Today paths are copied into `src/components/nav/shopExploreMenuTapPaths.generated.ts`.
 * Extend this script to parse an exported SVG and rewrite that file when ready.
 */
console.log(
  "generate-shop-menu-tap-paths: no SVG parser wired yet. Manually update shopExploreMenuTapPaths.generated.ts from Figma Menu Taps.svg."
);
process.exit(0);
