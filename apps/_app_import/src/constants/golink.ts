/** Merchant `_id` used when the user taps Shop Now on the home GoLink result dialog (in-app checkout flow). */
export const GOLINK_SHOP_NOW_OFFER_ID = "brand-orbit-airways-1003";

/** Query flag: shop detail auto-starts the tracked deeplink flow (LoadingShop → redirect). */
export const GOLINK_SHOP_CONTINUE_QUERY = "golinkContinue";
