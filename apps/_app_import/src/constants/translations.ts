/**
 * Temporary: force English message catalogs and hide locale-switching UI while
 * aligning screens with design. Set to `false` when Thai copy and the language
 * picker should work again.
 */
export const TRANSLATIONS_DISABLED = true;
