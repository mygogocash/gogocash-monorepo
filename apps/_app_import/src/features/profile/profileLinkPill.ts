/** Same visual as social “Link” actions (`SocialRow`) and profile CTAs that should match. */
export const profileLinkActionPillClass =
  "shrink-0 rounded-full bg-[#00CC99] px-4 py-2 text-xs font-medium text-white";
