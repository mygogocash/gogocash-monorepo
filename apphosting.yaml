# Firebase App Hosting – GoGoCash Admin (frontend + mock data only)
# See https://firebase.google.com/docs/app-hosting/configure
# Connect this repo in Firebase Console → App Hosting → Create backend

runConfig:
  minInstances: 0

# Optional: set NEXTAUTH_SECRET in Firebase Console (App Hosting → Backend → Environment)
# or: firebase apphosting:secrets:set NEXTAUTH_SECRET
# NEXTAUTH_URL is set automatically by App Hosting to your live URL.
# env:
#   - variable: NEXTAUTH_SECRET
#     secret: NEXTAUTH_SECRET
#     availability: [BUILD, RUNTIME]
