# GoGoCash Expo Migration — Live Context

_Last updated: 2026-06-01. Branch `expo-module` (pushed to origin, synced). Latest feature commits `54add48` → `a593f6a` → `265bb85` (category/header UI fixes + PostHog warning + web i18n label — see "Desktop category/header UI fixes" below); run `git rev-parse --short HEAD` for the exact tip rather than trusting a pinned SHA (see the ghost-SHA history below)._

What's done / what's left in the Next.js → Expo migration for `apps/mobile`. See `agent.md` for workflow, `design.md` for the design system, `project.md` for the repo map.

## Scope
Visual/structural parity with the frozen Next.js reference (`gogocash_app-staging`). i18n, web3, and Stripe checkout remain parked (verified genuinely absent). Audit caveat: the gap audit had a **~50% false-positive rate** — always ground-truth against BOTH the real web file and the real Expo file before fixing.

## BACKLOG TRUTH (adversarially deep-verified 2026-05-31 — supersedes the old "parked" framing)
A workflow verified every "parked by design" claim against real code. The old blanket "live-data, auth, i18n, analytics, web3, Stripe all parked" was WRONG for 3 of 6 — they understated shipped work. See memory `gogocash-expo-backlog-deepverify`. Corrected status:
- **auth = partially-wired** (NOT parked): session store + route guard + OAuth callback all RUN; only the login FORM is fake (hardcoded OTP `123456`). Finish-the-wiring.
- **live-data = partially-wired** (NOT parked): 6 account screens wire `useQuery` to real endpoints, dormant because `accountDataSource='fixtures'`. Home+Discovery truly fixture-only.
- **analytics = infra fully-wired**; SLICE 1 DONE (d1360f8): `src/analytics/events.ts` event vocabulary mirrors web GA4/PostHog names exactly (page_view, merchant_category_select, select_promotion, quest_started, cashback_withdraw_success, complete_registration + identify/reset, platform:"mobile"), 12 tests. SLICE 2 DONE (11af207): page_view wired app-wide via `src/analytics/RouteAnalyticsTracker.tsx` (render-null, mounted once in AppProviders, mirrors web RouteAnalyticsTracker) + `pageTracking.ts` (getMobilePageType byte-identical to web getPageTypeFromPathname; dedup; login_state from session.access_token) + `useAnalytics.ts` (usePostHog→MobileAnalyticsClient bridge). 8 tests. SLICE 3 DONE (5389171): `select_promotion` fires on home banner tap (HeroBannerLink in CustomerHomeScreen — one `<Link>` wraps main+side banners; onPress on the Link fires then navigates; props from real HomeHeroBanner fields id/placement/href, no name field so promotionName reuses id), 4 tests. The other 3 planned events are BLOCKED by parked functionality and deliberately NOT wired (would fabricate events for impossible actions): quest_started (quest task rows are static `<View>`, no pressable CTA), merchant_category_select (home has no category-tile mapping — zero `category.*` usage), cashback_withdraw_success (handleWithdraw only sets a local success message — no real payout; `withdraw/api.ts` imported by no screen). These unblock when the parked live-data/withdraw layers land. RouteAnalyticsTracker now HAS a render test (c4807b8 — mounts null, re-renders safe, no throw on the no-PostHog path). STILL PENDING: HeroBannerLink/Home not render-covered (the Home screen's heavy import graph would need more stubs). view_item/select_item/add_to_wishlist deferred (need parked merchant/offer model).
- **Stripe / web3 / i18n = correctly parked** (no deps even installed; `src/web3` does not exist).
- **HIDDEN pending (not previously tracked):** GoGoSense detector is a permanent no-op stub behind a full 8-route surface; credit-score renders fabricated numbers as real (`my-rating` is a bare redirect); `NativeParityScreen.tsx` (1615 lines) is dead/unrouted; control docs drift (agent.md 243/7 stale vs real 264/0; project.md repo map omits ~14 of 21 src dirs).

## State: everything below is committed AND pushed to `origin/expo-module` (last feature commit d1360f8)
Source suite **276 passed / 0 failed / 0 todo** (40 files, `npm test`) — +12 analytics vocabulary (slice 1), +8 page-view tracking (slice 2), +4 banner select_promotion wiring (slice 3). Render suite **24 passed** (`npm run test:render`) — CustomerRouteState (5) + RouteAnalyticsTracker (3) + CustomerAgeVerificationScreen (5) + CustomerWithdrawMethodScreen (6) + CustomerProfileOffersScreen (5). tsc **0 errors** (verify tsc separately — source-string parity tests do not compile components, so the suite can be green while tsc is red; a StyleSheet.absoluteFillObject TS2551 slipped through once and was fixed in 6de020e). NOTE: bash stdout/exit-codes were corrupted this session (rtk) — counts here were confirmed via the Read tool on raw vitest result files + JSON reporter, not bash echo.

Render-test harness (`npm run test:render`): **24 passed / 0 failed** across 3 components — mounts `CustomerRouteState` (5), `RouteAnalyticsTracker` (3), and `CustomerAgeVerificationScreen` (4, asserts the age-gate writes its verified flag to localStorage on confirm) in happy-dom (`react-native`→`react-native-web`). Render coverage is 5 of 31 screens (added CustomerProfileOffersScreen [267798e], the first useCustomerAccountResource-backed screen render-tested — mounted inside a QueryClientProvider since the hook calls useQuery unconditionally; resource returns status:ready from fixtures under the harness). (the safe-area-context stub unblocked AccountPageShell, so CustomerWithdrawMethodScreen [6ff9ef6] mounts; note its bank+account render in one <Text> with a ' · ' separator + nested <Text>, so those assertions use substring matchers). `src/test-support/` stubs: phosphor-react-native, posthog-react-native, expo-router, static assets, CustomerDesktopFooterSlot, and **react-native-safe-area-context** — each is a node-module/component whose value-position `typeof` type alias (or native shape) breaks the rolldown/oxc transform; all stubbed, NONE changed in app source. The safe-area-context stub (commit 2c4be85) is what unblocked AccountPageShell, hence the 14 shell screens, for render coverage. LESSON (cost real time this session): a render test that fails to TRANSFORM produces "0 tests" for that file, which is MASKED in the suite-level "N passed" aggregate — so a broken new test looks like it added nothing yet the run stays "green". ALWAYS confirm a new render test's OWN file count went up via `--reporter=json` per-file (or cross-check `it(` count in source), never trust the rolled-up total. Two false "12 passed" claims this session (e1921ef commit msg + a067eaa docs) came from exactly that masking; corrected to the true 8 in 46b54ef; a wrong footer-stub fix (659dd82, since reverted) mis-diagnosed the culprit; finally made genuinely 12 in 2c4be85 (safe-area-context was the real culprit). Separate `vitest.render.config.ts` + `*.render.test.tsx` glob; `src/test-support/` stubs for phosphor-react-native, posthog-react-native, expo-router, static assets, and CustomerDesktopFooterSlot — each is a node-module/component whose value-position `typeof` type alias or native shape breaks the rolldown/oxc transform; all stubbed, NONE changed in app source. Pattern for adding coverage: if a new transitive import trips "Unexpected token 'typeof'" or a resolve error, isolation-probe to find it (`import("@mobile/...")` one-liners) then alias a stub in the render config. NOTE: the harness was committed broken first (false "5 passed" claims in earlier commits) and only genuinely passed as of e53236c. See memory `gogocash-expo-render-test-harness`.

Visual-parity fixes:
1. Referral invitation table — 4th "Status" column + green Success pill (#E6F7ED / #00B14F, radius 30).
2. Bottom-nav — emphasized Wallet button 72→64px + lift −22→−32; profile avatar 34→28.
3. Category grid — added missing 5-column tier for lg widths 1024–1279 (web 2/3/4/5/6).
4. Quest leaderboard — "My Rank" card (#F1FFFC, #00CC99 border, 2 columns + View Points breakdown).
5. Verify-Phone heading color #00AA80 → #00B14F.
6. Footer column grid responsive — `getDesktopFooterGrid(viewportWidth)` + flexWrap/gap/flexBasis (cols 3→2→1, gap 64→32).
7. Inactive desktop subnav tabs — hover-underline (`DesktopCategoryTab`, onHoverIn/Out, opacity active?1:hovered?0.4:0).
8. Membership — #savings proof card ("Annual saves you real money", ฿588 vs ฿490, "You save ฿98 (~16%)") + #social-proof 3-stat row (220+/16%/฿49). Copy verbatim from web en.json.

Security suite — all 7 `it.todo` placeholders resolved into real tests (security-pentest.test.ts now 22 pass / 0 todo):
9. 4 client-contract tests over already-correct code: auth-callback replay (router.replace + code exchange), account IDOR (session-bound endpoints, no caller id), billing tampering (no client price/customer fields), GoGoSense screenshot (authenticated api client, no raw fetch).
10. 2 new real client modules: `src/withdraw/api.ts` (`createWithdrawApi.submitWithdrawal` requires + sends an `Idempotency-Key` header on POST /withdraw/submit) and `src/pdpa/api.ts` (`createPdpaApi.requestDataExport` session-bound, locale-only, no userId/accountId).
11. Device-lifecycle privacy: `src/security/PrivacyScreenGuard.tsx` (pure RN `AppState` overlay — covers the UI when state !== "active", i.e. iOS app switcher / backgrounded / crash-relaunch), wired into `AppProviders`. NOTE: `expo-screen-capture` install **timed out in this environment** (no network); the AppState overlay is the dependency-free implementation. To also get Android FLAG_SECURE / iOS recording prevention, install `expo-screen-capture` in a clean env and add `preventScreenCaptureAsync` to the guard.

## Confirmed FALSE POSITIVES (no change — Expo already matched web)
Wallet empty-title color (already #00AA80) · Wallet table chrome (web shows only NoDataWallet at 0 rows) · Category PolicyBanner/PolicyTerms (web data map empty + flag off) · Discovery grid columns (already 2/3/4/5).

## Quest History view — DONE (d4c8693, 2026-05-31)
Built the minimal-parity slice as an in-file `QuestHistoryView` in `CustomerQuestScreen.tsx`, branched via `if (history)` (the non-history tabs/leaderboard path is untouched, preserving the frontend-user-flow landmark `history ? "leaderboard" : "how-to-win"`). Sections: hero + plan card (kicker/title/intro/3 numbered steps/Quest+Stores CTAs), "This round" campaign card (period label + pending state, your-score sign-in hint + footnote), monthly section (empty state), rewards section (empty state). Copy is the EXACT web `gogoquestHistory*` next-intl English values (web uses 55 FLAT `gogoquestHistory*` keys, NOT nested `quest.history.*`), stored in a new `webQuestHistory` fixture in `webDesignParity.ts` and asserted byte-for-byte by `quest-history-parity.test.ts` (2 tests, TDD RED→GREEN).
- Web source: `gogocash_app-staging/src/features/quest/component/GogoquestHistory.tsx` (691) + `GogoquestHistoryInsightSection.tsx` (76) + `QuestHistoryNavLink.tsx` (88).
- STILL PARKED (live-data phase, not built): real campaign dates / day-left countdown / round-ended state, actual monthly bars, real rewards list with NEW badges, the month-picker leaderboard, and the player-summary dialog. The insight section was also skipped. These are deliberately out of the visual-parity slice.

## Quest-tasks note
A draft to add daily-reward + social-missions + invite blocks to the quest Tasks tab was scoped but its fixture copy was reconstructed, not verified — NOT committed. If picked up, re-extract exact labels from `gogocash_app-staging/src/features/quest/component/ListShop.tsx` + `common/SocailList.tsx` first.

## Full desktop parity audit — DONE (2026-05-31)
Swept all 45 desktop routes + mobile (390) Expo-vs-Next with Playwright DOM measurement: **ZERO genuine misalignments**. The one real bug found — home desktop header/footer were trapped in the `phoneFrame` 1440 cap instead of full-bleed — was fixed in `ce8de6a` (header/footer lifted out of the cap; content stays centered at the 1440 column; verified header L0–R2000 bar, content L280–R1720, footer full-bleed, matching staging). Every other workflow/sweep "misalignment" flag was a measurement false positive (carousels reporting child rects past a clipped viewport; off-screen animation drawers; geometry probes picking different DOM nodes on each app; RN-web internal-scroll footers measured pre-layout). See memory `gogocash-expo-parity-fullaudit` + `gogocash-expo-navbar-footer-align`. (Note: `node_modules` was deleted mid-session by a disk cleanup and reinstalled to finish the audit.)

## Desktop category/header UI fixes — DONE (2026-06-01)
Three reported desktop UI bugs on the category page, all ground-truthed against the live Next reference and fixed:
- **Active nav state** (`CustomerDesktopHeader.tsx`): the top-nav green underline was pinned to "Top Brands" because `webDesktopHeaderNavItems` hardcodes `active:true`. Now derived from the route via `usePathname()` + `isDesktopNavItemActive()` (decode+normalize, exact-match for "/"); passed to `DesktopCategoryTab` + the nav icon. The left Categories sidebar already derived active from the route param. Verified: on `/category/Health & Beauty` only that tab underlines + that sidebar row highlights; on `/category/Travel` only Travel; Top Brands only on "/".
- **Sort pills** (`CustomerCategoryDetailScreen.tsx`): font was 16px/weight-dropped vs Next's 14px/600/lineHeight20/padding6×16 — set `sortPillText` to 14/600/20 and `sortPill` to padding 6×16 (uniform; dropped the 22px lowest override). Verified live: all four pills 14/600/lh20.
- **Focus highlight box** (RN-web): logo + header pressables (logo/quest/sign-in) and the category sidebar items + sort pills left a persistent CSS focus outline after a mouse click (RN-web doesn't gate the ring behind `:focus-visible`). Added a `webPressableFocusReset` (`outlineStyle/Width:0`) to those pressables; a11y role/label unchanged. GOTCHA: `<Link asChild>`-wrapped pressables (logo/quest/sign-in, and `CategoryNavItem`) need `StyleSheet.flatten([...])` — an array style to a `<Slot>` child crashes expo-router at runtime (blank red overlay) while the vitest gates still pass, so this is only caught by live page load. See memory `gogocash-expo-rnweb-ui-gotchas`.
- **Sort label copy** "High Cashback" → "Highest Cashback": changed in `webDesignParity.ts` (category + discovery `sortPills`) AND the 3 web i18n locale files (`src/messages/{en,th,jp}.json` key `discoverSortHighCashback`, which backs the Next category/discover/shop sort pills) so web + Expo stay in sync. Parity tests updated to the new label. NOTE: this is an intentional product copy change — the prior Next reference rendered "High Cashback".
- **PostHog keyless dev-error toast** (`AppProviders.tsx` + `useAnalytics.ts`): `usePostHog()` console.error'd "usePostHog was called without a PostHog client..." once per caller because `AppProviders` skipped `<PostHogProvider>` entirely when no `posthogKey` is configured (the normal local/web-dev path). Now ALWAYS mounts the provider — real client when keyed, else a no-op client with `autocapture={false}` (no PostHog instance, no network). `useAnalytics` keeps using `usePostHog()` (returns the no-op client → null on that path, so events still no-op). Verified live: zero `usePostHog` console errors across home/category/discover; no toast.
- Commits: `54add48` (the 3 UI bugs + label) → `a593f6a` (PostHog + th/jp labels) → `265bb85` (en label; a593f6a had missed en because an Edit silently failed to persist — caught by post-commit `git show --stat` + live re-check). Each gated render+source+tsc all 0 before commit.
