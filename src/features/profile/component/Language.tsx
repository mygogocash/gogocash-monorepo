"use client";

import AccountSettingsView from "./AccountSettingsView";

/** Profile `/language` — Account settings hub (Figma Account Settings; route kept for existing links). */
const Language = () => <AccountSettingsView />;

export default Language;
